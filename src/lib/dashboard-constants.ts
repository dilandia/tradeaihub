/**
 * Constantes do dashboard — tamanhos padronizados.
 *
 * - KPI_CARD_HEIGHT_PX: altura dos cards da linha superior (KPIs)
 * - STANDARD_CARD_HEIGHT_PX: altura dos cards do grid principal (1 col × 1 linha)
 * - TOP_ROW_MAX_ITEMS: máximo de widgets na top row (2 linhas × 4 colunas)
 */
export const KPI_CARD_HEIGHT_PX = 126;
export const STANDARD_CARD_HEIGHT_PX = 315;
export const TOP_ROW_MAX_ITEMS = 8;
